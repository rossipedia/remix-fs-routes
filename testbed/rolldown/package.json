{
  "name": "remix-fs-routes-testbed-rolldown",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rolldown --config",
    "test": "node dist/bundle.mjs",
    "check": "pnpm build && pnpm test"
  },
  "devDependencies": {
    "remix-fs-routes": "workspace:*",
    "rolldown": "1.2.5"
  }
}
