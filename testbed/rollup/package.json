{
  "name": "remix-fs-routes-testbed-rollup",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "test": "node dist/bundle.mjs",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "rollup": "^4.62.5"
  }
}
