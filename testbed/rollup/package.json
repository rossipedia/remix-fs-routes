{
  "name": "remix-fs-routes-testbed-rollup",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "test": "node dist/bundle.mjs",
    "check": "pnpm build && pnpm test"
  },
  "devDependencies": {
    "remix-fs-routes": "workspace:*",
    "rollup": "^4.62.5"
  }
}
